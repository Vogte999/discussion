<%= simple_form_for User.new, url: "/users/#{session[:id]}", method: :put do |f| %>
<%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.button :submit, "update"%>
<% end %>
